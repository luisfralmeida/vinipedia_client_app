import styled from 'styled-components';

export const StyledContainer = styled.main`
    min-height: 100vh;
    width: 100%;
    background: #fafafa;
    display: flex;
    position: relative;
`